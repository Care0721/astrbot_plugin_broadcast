name: astrbot_plugin_broadcast # 插件唯一识别名，以 astrbot_plugin_ 前缀开头
display_name: 广播插件 # 用于展示的名字，可以是方便人类阅读的名字（需要版本 >= v4.5.0，低版本不会报错，请放心填写）
desc: 支持私聊广播到全部/指定群聊，定时广播，@全体成员，历史记录，黑名单，权限控制，Markdown/图片，撤回，分段发送 # 插件简短描述
version: v1.3.0 # 插件版本号。格式：v1.1.1 或者 v1.1
author: Care # 作者
repo: https://github.com/Care0721/astrbot_plugin_broadcast # 插件的仓库地址
